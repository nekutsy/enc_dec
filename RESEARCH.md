# RESEARCH.md — Архив исследований по автоэнкодеру

> ⚠️ **Все результаты ниже получены на старом датасете (~23M символов).**
> Текущий датасет (~50M символов) даёт другие абсолютные значения loss,
> но архитектурные закономерности качественно сохраняются.

---

## Этап 1: Сетка (sweep_summary.csv)

**Настройки:** seq_len ∈ {1,2}, hidden = 4× input_dim, n_hidden ∈ {1..6},
150M символов, 13 эпох, sliding window.

### seq_len=1 (input=21, hidden=84)
| n | params | val_loss |
|---|--------|----------|
| 1 | 4,222 | 0.0300 |
| 2 | 18,838 | 0.0174 |
| 3 | 33,454 | 0.0086 |
| 4 | 48,070 | 0.00273 |
| 5 | 62,686 | 0.00094 |
| 6 | 77,302 | **0.00043** |

→ Чем глубже, тем лучше. При seq_len=1 коллапса нет ни при каком n.

---

## Этап 2: Бинарный поиск (sweep_binary_summary.csv)

**Настройки:** hidden = 4× input_dim, 120M символов, sliding window.
Бинарный поиск оптимального n_hidden для каждого seq_len.

### Результаты (120M символов)

| seq_len | input | hidden | n_opt | val_loss | params |
|---------|-------|--------|-------|----------|--------|
| 2 | 42 | 168 | 8 | 0.00024 | 418K |
| 4 | 84 | 336 | 14 | 0.00179 | 3.0M |
| 8 | 168 | 672 | 9 | 0.00270 | 7.5M |
| 16 | 336 | 1,344 | 8 | 0.00905 | 26.3M |
| 32 | 672 | 2,688 | 6 | 0.01589 | 76.1M |
| 64 | 1,344 | 5,376 | 3 | 0.03110 | 130.8M |
| 128 | 2,688 | 10,752 | 1 | 0.05581 | 60.6M |

### Закономерности

1. **n_opt монотонно убывает с ростом seq_len**: 14 → 9 → 8 → 6 → 3 → 1.
   Чем длиннее контекст, тем меньше слоёв нужно.

2. **Базовый уровень (n=1) стабилен**: val ≈ 0.037–0.039 для всех seq_len от 2 до 64.
   Один слой — это «потолок» для любого seq_len, примерно одинаковый.

3. **Коллапс при перегрузке**: n=16 при seq_len=32 даёт 221M параметров → val=0.080.
   Модель слишком большая для задачи, не сходится.

4. **seq_len=128**: даже n=1 — 60M параметров, val=0.056. Задача сложная,
   но возможно недостаточно данных (всего ~234K окон).

---

## Этап 3: Width sweep (width_sweep_summary.csv)

**Настройки:** оптимальное n из бинарного поиска, b ∈ {1/7, 1/3, 1, 2, 4, 8},
120M символов.

### seq_len=8 (n_opt=9)

| b | hidden | val_loss |
|---|--------|----------|
| 1/7 | 24 | 0.0628 |
| 1/3 | 56 | 0.0470 |
| 1 | 168 | 0.0353 |
| 2 | 336 | 0.0288 |
| 4 | 672 | 0.0239 |
| 8 | 1,344 | 0.0058 ★ |

### seq_len=16 (n_opt=8)

| b | hidden | val_loss |
|---|--------|----------|
| 1/7 | 48 | 0.0731 |
| 1/3 | 112 | 0.0650 |
| 1 | 336 | 0.0571 |
| 2 | 672 | 0.0463 |
| 4 | 1,344 | 0.0228 |

### seq_len=4 (n_opt=14)

| b | hidden | val_loss |
|---|--------|----------|
| 1/7 | 12 | 0.0543 |
| 1/3 | 28 | 0.0377 |

### Ключевой вывод: шире = лучше

При фиксированном количестве слоёв увеличение ширины всегда улучшает качество.
b=8 (hidden=1344) даёт val=0.0058 против 0.024 при b=4 — улучшение в 4 раза.

**Ограничение:** VRAM. При seq_len=16, b=8 hidden=2688, n=8 → 105M параметров,
уже не влазит в 8GB. Width sweep на больших seq_len упёрся в память.

---

## Этап 4: Соотношение n/b (ratio sweep, старый датасет)

**Настройки:** seq_len=32, 80M параметров (фиксированный бюджет),
b подбирается бинарным поиском под заданное n.

### 80M, seq_len=32

| n | b | hidden | val_loss |
|---|-----|--------|----------|
| 4 | 5.26 | 3,535 | 0.0278 |
| 6 | 4.10 | 2,758 | 0.0261 |
| 7 | 3.75 | 2,522 | **0.0237** ★ |

→ При фиксированном бюджете оптимален компромисс: n=7, b≈3.75.
Слишком мало слоёв (n=4) или слишком узко — хуже.

---

## Общие выводы из старых тестов

### 1. Оптимальное n зависит от seq_len

```
seq_len:   1    2    4    8   16   32   64  128
n_opt:     6+   8   14    9    8    6    3    1
```

Короткий контекст → много слоёв (глубокая компрессия).
Длинный контекст → мало слоёв (поверхностная обработка, больше «проходная»).

### 2. Ширина бьёт глубину при равном бюджете

Width sweep показал: b=8, n=9 (7.5M params) → val=0.0058.
Binary search: b=4, n=9 (7.5M params) → val=0.0027 (120M символов).

Но width sweep и binary sweep — РАЗНЫЕ эксперименты с разными настройками,
так что прямое сравнение некорректно. Ratio sweep (80M, фикс. бюджет)
подтверждает U-образную форму: есть оптимум, а не монотонность.

### 3. Коллапс при слишком большом количестве параметров

Модели > ~200M параметров при seq_len=32 перестают сходиться (val → 0.08).
Причина: 120M символов при 200M+ параметров → недостаточно данных для обучения,
модель переобучается или расходится.

### 4. Базовый уровень n=1 удивительно стабилен

Независимо от seq_len (2–64), один слой даёт val ≈ 0.037–0.039.
Это «потолок» линейной модели (один слой с BatchNorm = аффинное преобразование
+ нелинейность активации).

### 5. seq_len=2 — особый случай

При seq_len=2 оптимальное n=8 при b=4 дало val=0.00024 — на порядок лучше seq_len=4.
Возможно, пара символов (2×21=42 бита) слишком простая задача для автоэнкодера.

---

## Что изменилось в текущих тестах (июнь 2026)

По сравнению с этапами 1–3 (сетка/бинарный поиск/width sweep):

1. **Архитектура**: прямоугольная (все слои одинаковой ширины) вместо hourglass
2. **Фиксированный бюджет параметров**: 20M/40M/80M вместо переменного (этап 4 — ratio sweep)
3. **Batch size как переменная**: обнаружено, что bs критически влияет на качество
4. **Текущие результаты**: см. `memory/2026-06-15.md`

> ⚠️ Ratio sweep (этап 4) тоже на старом датасете (~23M).
> Новый датасет (~50M) пока не использовался ни в одном sweep.

---

## Хронология файлов

| Файл | Дата | Статус |
|------|------|--------|
| `sweep_summary.csv` | 14 июня | Старый датасет, seq_len=1-2, сетка — **архивировано** |
| `sweep_binary_summary.csv` | 14 июня | Старый датасет, бинарный поиск n по seq_len — **архивировано** |
| `width_sweep_summary.csv` | 14 июня | Старый датасет, вариация b — **архивировано** |
| `ratio_sweep_summary.csv` | 14 июня | 80M ratio sweep (старый датасет) |
| `ratio20_sweep_summary.csv` | 15 июня | 20M ratio sweep (старый датасет) |
| `ratio40_sweep_summary.csv` | 15 июня | 40M ratio sweep (старый датасет) |
